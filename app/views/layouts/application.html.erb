<!DOCTYPE html>
<html>
<head>
  <title>YouriCv</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- jQuery -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>

  <!-- Popper.js -->
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>

  <!-- CSS de Bootstrap -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= javascript_include_tag "application", type: "module" %>
  <link href='https://fonts.googleapis.com/css?family=Lato' rel='stylesheet'>
</head>

<body>

<nav class="navbar navbar-expand-lg navbar-light navbar-fixed">
  <div class="container-fluid">
    <!-- Groupe de gauche -->
    <div class="navbar-header me-auto">
      <i class="fa-solid fa-code fa-fade fa-2xl" style="color: #000000;"></i>
    </div>

    <!-- Bouton pour le menu déroulant (version mobile) -->
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Groupe central -->
    <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
      <ul class="navbar-nav mx-auto">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Skills", skills_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Work-Experiences", work_experiences_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Degrees", degrees_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Contact", new_contact_path, class: "nav-link" %>
        </li>
      </ul>
    </div>

    <!-- Groupe de droite -->
    <div class="ml-auto">
      <ul class="navbar-nav">
        <li class="nav-item">
          <% if user_signed_in? %>
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "btn-log nav-link" %>
          <% else %>
            <%= link_to "Login", new_user_session_path, class: "btn-log nav-link" %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>


  <!-- Affichage des messages flash -->
  <% if notice %>
    <div class="alert alert-success">
      <%= notice %>
    </div>
  <% elsif alert %>
    <div class="alert alert-danger">
      <%= alert %>
    </div>
  <% end %>

  <%= yield %>

  <footer class="bg-dark text-center text-white">
    <div class="container p-4 pb-0">
      <section class="mb-4">
        <!-- Vos icônes pour les réseaux sociaux -->
        <a href="https://www.linkedin.com/in/youri-soposki-424668218/" target="_blank" role="button"><i class="fa-brands fa-linkedin fa-flip fa-2xl" style="color: #ffffff;"></i></a>
        <a href="https://github.com/YouriSpk" target="_blank" role="button"><i class="fa-brands fa-square-github fa-spin fa-spin-reverse fa-2xl" style="color: #ffffff;"></i></a>
      </section>
    </div>
    <div class="text-center p-3" style="background-color: rgba(0, 0, 0, 0.2); font-size: 10px;">
      © 2024 Youri Soposki - All right reserved
    </div>
  </footer>

  <!-- JavaScript de Bootstrap -->
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>

  <!-- Votre CSS personnalisé -->
  <style>
    @import url('https://fonts.googleapis.com/css?family=Cutive+Mono');

    *,
    *:before,
    *:after {
      box-sizing: border-box;
    }

    html {
      font-size: 18px;
    }

    body {
      font-family: "Cutive Mono", serif;
      font-size: 1.2em;
      line-height: 1.6;
      background: #d6d2d2;
      overflow-x: hidden;
      margin-top: 70px; /* Ajout de cette marge pour décaler le contenu en dessous de la navbar */
    }

    .navbar-fixed {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      z-index: 1000; /* Assurez-vous que le z-index est supérieur à celui des autres éléments pour qu'il apparaisse au-dessus */
    }

    .container {
      position: relative; /* Ajout de position relative */
      z-index: 1; /* Assurez-vous que le z-index est supérieur à celui de la navbar */
    }
  </style>

</body>
</html>
